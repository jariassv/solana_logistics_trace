/**
 * Layout raíz del área `/panel`: las rutas con rail operativo viven en `(operativo)`.
 */
export default function PanelLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    return children;
}
